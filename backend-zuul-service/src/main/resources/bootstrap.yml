spring:
  application:
    name: zuulservice
  profiles:
    active:
      default
  cloud:
    config:
      enabled: true
      uri: http://localhost:8888

# TODO remove
management.security.enabled: false
